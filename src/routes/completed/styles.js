import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  floatingActionBtn: {
    margin: 0,
    top: 'auto',
    right: 40,
    bottom: 40,
    left: 'auto',
    position: 'fixed',
  },
}));
